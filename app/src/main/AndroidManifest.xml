<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ke.jakinda.APR.PLATEREC">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CLEAR_APP_CACHE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-feature
        android:name="android.hardware.location"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_GPS" />

    <application
        android:name="com.activeandroid.app.Application"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="parking.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />
        <meta-data
            android:name="AA_MODELS"
            android:value=" ke.jakinda.APR.PLATEREC.activities.models.Logs,ke.jakinda.APR.PLATEREC.activities.models.Parking_Time ,
            ke.jakinda.APR.PLATEREC.activities.models.Parking_Number , ke.jakinda.APR.PLATEREC.activities.models.RegistrationDetails , ke.jakinda.APR.PLATEREC.activities.models.Admin ,
             ke.jakinda.APR.PLATEREC.activities.models.Events" />

        <activity android:name=".activities.activities.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.config.OCRActivity" />
        <activity android:name=".activities.activities.SignUp" />
        <activity android:name=".activities.config.MainActivity" />
        <activity android:name=".activities.activities.registration_Form" />

        <activity android:name=".activities.activities.ReportActivity" />



    </application>

</manifest>